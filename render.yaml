services:
  - type: web
    name: hotel-frontend
    runtime: node
    region: oregon
    plan: free
    rootDir: hotel-frontend
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
